<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">
    <data>

        <import
            alias="Converter"
            type="com.example.waveclone.binding.BindingConverterKt" />

        <variable
            name="viewModel"
            type="com.example.waveclone.ui.TransactionInfoViewModel" />
    </data>
    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <include
            android:id="@+id/progress"
            layout="@layout/layout_progress"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:isVisible="@{Converter.setViewRender(viewModel.showProgress)}"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <androidx.appcompat.widget.AppCompatTextView
            android:id="@+id/tv_lbl_one"
            android:layout_height="wrap_content"
            android:layout_width="0dp"
            android:textSize="@dimen/text_size_16"
            android:textStyle="bold"
            android:textColor="@color/dark_grey"
            android:layout_marginStart="@dimen/_10dp"
            android:layout_marginTop="@dimen/_10dp"
            android:layout_marginBottom="@dimen/_10dp"
            android:text="@string/amount_send"
            app:layout_constraintBottom_toTopOf="@id/rv"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintEnd_toStartOf="@id/tv_lbl_two"
            app:layout_constraintWidth_percent="0.55"
            tools:text="Amount to send"  />

        <androidx.appcompat.widget.AppCompatTextView
            android:id="@+id/tv_lbl_two"
            android:layout_height="wrap_content"
            android:layout_width="0dp"
            android:layout_marginEnd="@dimen/_10dp"
            android:textSize="@dimen/text_size_16"
            android:textColor="@color/dark_grey"
            android:textStyle="bold"
            android:gravity="end"
            android:text="@string/transaction_fee"
            app:layout_constraintStart_toEndOf="@id/tv_lbl_one"
            app:layout_constraintTop_toTopOf="@id/tv_lbl_one"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintWidth_percent="0.4"
            tools:text="Transaction Fee"  />

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/rv"
            android:layout_marginTop="@dimen/_10dp"
            android:layout_marginHorizontal="@dimen/_10dp"
            android:layout_width="match_parent"
            android:visibility="@{Converter.setViewRender(!viewModel.showProgress)}"
            app:layout_constraintStart_toStartOf="@id/tv_lbl_one"
            app:layout_constraintEnd_toEndOf="@id/tv_lbl_two"
            app:layout_constraintTop_toBottomOf="@id/tv_lbl_one"
            android:layout_height="wrap_content"
            android:clipToPadding="false"
            items="@{viewModel.transactionInfoData}"
            app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"
            tools:itemCount="2"
            tools:listitem="@layout/item_transaction_info" />

        <androidx.appcompat.widget.AppCompatButton
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:elevation="@dimen/_5dp"
            app:layout_constraintWidth_percent="0.9"
            android:layout_marginBottom="@dimen/_20dp"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            android:textAllCaps="false"
            android:background="@drawable/custom_button"
            android:textColor="@color/white"
            android:text="@string/send"
            />

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>